<!DOCTYPE html>
<html>
<head>
    <title>NORDSAGA - Лидеры</title>
    <style>
        body {
            background: #0a0c10;
            color: #ffd700;
            font-family: Arial;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #1a1e26;
            padding: 30px;
            border: 3px solid #b8860b;
            border-radius: 10px;
        }
        h1 {
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th {
            background: #b8860b;
            color: #000;
            padding: 10px;
        }
        td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #b8860b;
        }
        a {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #b8860b;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🏆 ЛИДЕРЫ 🏆</h1>
        <table>
            <tr>
                <th>Место</th>
                <th>Имя</th>
                <th>Слава</th>
                <th>Золото</th>
            </tr>
            <% users.forEach((user, index) => { %>
            <tr>
                <td>#<%= index + 1 %></td>
                <td><%= user.username %></td>
                <td><%= user.glory %></td>
                <td><%= user.gold %></td>
            </tr>
            <% }) %>
        </table>
        <a href="/">На главную</a>
    </div>
</body>
</html>